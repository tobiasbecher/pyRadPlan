version: "2"

build:
  os: "ubuntu-latest"
  tools:
    python: "3.x"

python:
  install:
    - method: pip
          path: .
    - requirements: docs/requirements.txt


sphinx:
  configuration: docs/conf.py
